# decrypt_openfirepass
Python project to decrypt administrator password extracted from openfire embedded-db.

Usage: `python script.py <ciphertext> <key>`

Requirements: pycryptodome

`pip install pycryptodome`